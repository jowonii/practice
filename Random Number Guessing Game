import random

number = random.randint(1, 100)
while True:
    guess = int(input("1부터 100까지 숫자를 맞춰보세요: "))
    if guess > number:
        print("너무 높아요!")
    elif guess < number:
        print("너무 낮아요!")
    else:
        print("정답입니다!")
        break
